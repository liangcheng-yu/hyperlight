# simpleguest in rs

This is simpleguest, but written in rust. Here's how to build it:

```sh
# on Windows:
make build-windows
make link-windows
make update-windows

# on Linux:
make build-linux
make link-linux
make update-linux
```