[package]
name = "simpleguest"
version = "0.4.0"
edition = "2021"

[dependencies]
hyperlight_guest = { path = "../../../hyperlight_guest" }
hyperlight_common = { path = "../../../hyperlight_common", default-features = false }
log = {version = "0.4", default-features = false }
